// sourcery:inline:auto:<%- serializable.name %>.MeowVapor
    /// A session identifier used in a cookie
    public private(set) var sessionToken: String = <%-serializable.name%>.generateSessionToken()
// sourcery:end
